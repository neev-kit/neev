name: Release

on:
  push:
    tags:
      # Production releases: v1.0.0, v2.1.0, etc
      # Auto-created by auto-release.yml when merging to main
      # Pattern: major.minor.patch (no pre-release suffixes)
      - 'v[0-9]+.[0-9]+.[0-9]+$'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.25.6'

      - name: Run tests
        run: |
          cd cli && go test ./... 
          cd ../core && go test ./...

      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@v5
        with:
          version: latest
          args: release --clean
          workdir: .
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
